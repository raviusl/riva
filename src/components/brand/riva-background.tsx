/**
 * @deprecated Import from `@/components/layout/riva-background` (Project 068).
 * Kept as a thin re-export to avoid duplicate background implementations.
 */
export { RivaBackground } from "@/components/layout/riva-background";
