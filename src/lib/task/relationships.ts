/**
 * Project 010 relationship surfaces (catalog / hierarchy references).
 * Distinct from the shared Task domain entity in `@/core/task`.
 */
export type {
  TaskAssignee,
  TaskDependency,
  TaskDependencyRelationshipType,
  TaskReference,
} from "@/types/task";
